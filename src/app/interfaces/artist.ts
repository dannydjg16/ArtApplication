import Artwork from './artwork';

export default interface Artist{
    id: number;
    name: string;
    bornLocation: string;
    biography: string;
    pictureURL: string;
    born: string;
    died: string;
}